name: List Actifacts
on:
  workflow_run:
    workflows: ["Update pull-request body"]
    types: [completed]

jobs:
  update-pr:
    runs-on: ubuntu-latest
    steps:
      - name: Print variables
        run: |
          echo "Variables github"
          echo ${{ github }}
          echo "---------------------------"
          echo ${{ github.context }}
          echo "---------------------------"
          echo ${{ github.context.payload }}
          echo "---------------------------"
          echo ${{ github.context.payload.pull_request }}
          echo "---------------------------"
          echo ${{ github.context.payload.pull_request.number }}
#      - name: Update PR Body.
#        uses: chiaretto/github-action-concat-pr-body@branch1
#        with:
#          github-token: "${{ secrets.GITHUB_TOKEN }}"
#          message: "TESTE"
#          replace-last-message: true